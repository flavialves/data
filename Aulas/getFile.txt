/* MUDAR OR NOME DO SCHEMA de [ixxxxxx] para o vosso número de aluno
	SourceTable - Tabela de Origem dos dados a Extrair
	DestinationTable - Tabela/Ficheiro de destino dos dados a escrever
*/
create table [ixxxxxx].ConfigParameters (SourceTable varchar(8000),DestinationTable varchar(8000),lastExecution datetime);
GO
insert [ixxxxxx].ConfigParameters values('DimGeography','DimGeography',null)
insert [ixxxxxx].ConfigParameters values('DimCustomer','DimCustomer',null)
GO
Select * from [ixxxxxx].ConfigParameters
GO
CREATE procedure [ixxxxxx].[ADFUpdateLastExecution] ( @dest varchar(8000)) as 
update [ixxxxxx].ConfigParameters set lastExecution =  getdate() where DestinationTable = @dest;
GO
